package goku_labels

// constlabel
const (
	Cluster = "cluster"
	Instance = "instance"
)

// api labels
const  (
	API = "api"
	Strategy = "strategy"
	Status ="status"

)
// proxy
const (
	Proto = "proto"
	Method = "method"
	Host ="host"
	Path = "path"

)